pub mod term;
pub mod glyph;

/*
use std::path::Path;

use freetype::Library;

fn load_font<'a, F>(display: &'a F, ft_lib: &mut Library, path: Path) -> io::Result<glyph::Map>
  where F: 'a + glium::backend::Facade {

}
*/
